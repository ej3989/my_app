# Copy this file to wifi_credentials.conf and replace the values. The copied
# file is ignored by Git. Add it as EXTRA_CONF_FILE when configuring the build.
CONFIG_APP_WIFI_SSID="your-ssid"
CONFIG_APP_WIFI_PSK="your-password"
